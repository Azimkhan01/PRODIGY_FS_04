<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Chatting Zone</title>
    <link rel="stylesheet" href="/public/css/chat.css">
    <style>
        /* Chat container styles */
        .chat-container {
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
            border: 1px solid #ccc;
            border-radius: 10px;
            background-color: #f9f9f9;
        }

        /* Join notification animation */
        .join-notification {
            text-align: center;
            margin-bottom: 10px;
            padding: 10px;
            background-color: #e0ffe0;
            color: #4CAF50;
            font-weight: bold;
            border-radius: 5px;
            animation: slideIn 1s ease-out forwards;
            opacity: 0;
        }

        @keyframes slideIn {
            from {
                transform: translateY(-100%);
                opacity: 0;
            }
            to {
                transform: translateY(0);
                opacity: 1;
            }
        }

        /* Chat box styles */
        .chat-box {
            max-height: 400px;
            overflow-y: auto;
            margin-bottom: 20px;
            padding-right: 10px;
        }

        .message {
            display: flex;
            flex-direction: column;
            margin-bottom: 15px;
            padding: 10px;
            border-radius: 10px;
        }

        .received {
            align-self: flex-start;
            background-color: #e0e0e0;
        }

        .sent {
            align-self: flex-end;
            background-color: #d1ecf1;
        }

        .name {
            font-weight: bold;
            margin-bottom: 5px;
        }

        .time {
            font-size: 0.8em;
            color: #888;
            align-self: flex-end;
        }

        /* Message input styles */
        .message-input {
            display: flex;
            justify-content: space-between;
        }

        .message-input input {
            flex: 1;
            padding: 10px;
            border-radius: 5px;
            border: 1px solid #ccc;
            margin-right: 10px;
        }

        .message-input button {
            padding: 10px 15px;
            border: none;
            border-radius: 5px;
            background-color: #4CAF50;
            color: white;
            cursor: pointer;
        }

        /* User count and list styles */
        .user-info {
            margin-bottom: 15px;
            font-weight: bold;
            font-size: 1.1em;
            display: flex;
            justify-content: space-between;
        }

        .user-count {
            color: #4CAF50;
        }

        .user-list {
            font-size: 0.9em;
            color: #555;
        }

        /* Responsive design */
        @media (max-width: 600px) {
            .chat-container {
                padding: 10px;
            }

            .message-input input {
                margin-right: 5px;
            }

            .message-input button {
                padding: 10px;
            }
        }
    </style>
</head>
<body>
    <div class="chat-container">
        <!-- User Info Section -->
        <div class="user-info">
            <span class="user-count">Users: <span id="userCount">2</span></span>
            <span class="user-list" id="user-list"></span>
        </div>

        <!-- Join Notification -->
        <div class="join-notification" id="joinNotification">🚀 John has joined the chat</div>

        <!-- Chat Box -->
        <div class="chat-box" id="chatBox">
           
            <!-- Add more messages as needed -->
        </div>

        <!-- Message Input -->
        <div class="message-input">
            <input type="text" placeholder="Type a message..." id="sendmsg">
            <button id="submit">Send</button>
        </div>
    </div>

    <script src="/socket.io/socket.io.js"></script>
    <script src="/public/js/chat.js"></script>
   
</body>
</html>
